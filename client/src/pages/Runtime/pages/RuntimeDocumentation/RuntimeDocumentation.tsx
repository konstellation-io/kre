import React from 'react';
import styles from './RuntimeDocumentation.module.scss';

type Props = {
  history: History;
  location: Location;
};
function RuntimeDocumentation({ history, location }: Props) {
  return (
    <>
      Runtime Documentation
    </>
  );
}

export default RuntimeDocumentation;
