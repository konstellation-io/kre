[request_definition]
r = role, grants, res

[policy_definition]
p = role, act

[role_definition]
g = _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = isAdmin(r.role) || hasGrantForResource(r.grants, r.res, p.act)
