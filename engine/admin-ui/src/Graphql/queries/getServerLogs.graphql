query GetServerLogs($filters: LogFilters!, $versionId: ID!, $cursor: String) {
  logs(filters: $filters, versionId: $versionId, cursor: $cursor) {
    items {
      id
      date
      nodeId
      nodeName
      workflowId
      workflowName
      message
      level
    }
    cursor
  }
}
