mutation CreateVersion($input: CreateVersionInput!) {
  createVersion(input: $input) {
    id
    name
    description
    status
    creationDate
    creationAuthor {
      id
      email
    }
  }
}
