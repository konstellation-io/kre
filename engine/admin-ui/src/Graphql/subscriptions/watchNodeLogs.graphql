subscription GetLogs($filters: LogFilters!, $versionId: ID!) {
  watchNodeLogs(filters: $filters, versionId: $versionId) {
    id
    date
    nodeId
    nodeName
    workflowId
    workflowName
    message
    level
  }
}
