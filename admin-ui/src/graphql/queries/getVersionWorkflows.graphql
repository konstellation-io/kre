query GetVersionWorkflows($versionId: ID!) {
  version(id: $versionId) {
    name
    status
    creationDate
    workflows {
      id
      name
      nodes {
        id
        name
        status
      }
      edges {
        id
        fromNode
        toNode
      }
    }
    configurationCompleted
  }
}
