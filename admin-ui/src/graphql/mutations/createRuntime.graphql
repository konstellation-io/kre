mutation CreateRuntime($input: CreateRuntimeInput!) {
  createRuntime(input: $input) {
    id
    name
    status
    creationDate
    creationAuthor {
      id
      email
    }
    publishedVersion {
      status
    }
  }
}
