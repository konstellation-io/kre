mutation CreateRuntime($input: CreateRuntimeInput!) {
  createRuntime(input: $input) {
    errors {
      code
      message
    }
    runtime {
      id
      name
      status
      creationDate
      creationAuthor {
        id
        email
      }
      publishedVersion {
        status
      }
    }
  }
}
