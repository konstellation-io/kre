mutation CreateRuntime($input: CreateRuntimeInput!) {
  createRuntime(input: $input) {
    id
    name
    description
    status
    creationDate
    creationAuthor {
      id
      email
    }
    publishedVersion {
      status
    }
  }
}
