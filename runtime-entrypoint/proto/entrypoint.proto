syntax = "proto3";

package entrypoint;

service EchoService {
    rpc Ping (PingRequest) returns (PingResponse) {}
}

message PingRequest {
    string name = 1;
    int32  nodes = 2;
}

message PingResponse{
    bool success = 1;
}
